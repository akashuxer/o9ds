import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'scroll-bar',
  title: 'Scrollbar',
  description:
    'Themed scroll affordance for overflow regions — consistent thumb and track styling aligned with Arvo tokens across light and dark surfaces.',
  componentSlug: 'scroll-bar',
})
