import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'splitter',
  title: 'Splitter',
  description:
    'Draggable resize rail between adjacent panes — used in side panels, master-detail layouts, and split views to adjust column width.',
  componentSlug: 'splitter',
})
