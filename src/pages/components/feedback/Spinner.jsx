import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'spinner',
  title: 'Spinner',
  description:
    'Compact indeterminate progress indicator for inline, button, and overlay loading states where completion time is unknown.',
  componentSlug: 'spinner',
})
