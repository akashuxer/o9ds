import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'multi-select',
  title: 'Multi Select',
  description:
    'Dropdown for choosing multiple values from a list — tags selected items in the trigger and supports search in long option sets.',
  componentSlug: 'multi-select',
})
