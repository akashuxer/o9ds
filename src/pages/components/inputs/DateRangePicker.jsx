import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'date-range-picker',
  title: 'Date Range Picker',
  description:
    'Start and end date selection for reporting windows, filter ranges, and booking periods — with calendar range highlighting.',
  componentSlug: 'date-range-picker',
})
