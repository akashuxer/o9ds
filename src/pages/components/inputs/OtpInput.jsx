import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'otp-input',
  title: 'OTP Input',
  description:
    'One-time password or verification code entry — segmented single-character fields with paste support and auto-advance.',
  componentSlug: 'otp-input',
})
