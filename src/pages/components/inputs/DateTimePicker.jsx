import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'date-time-picker',
  title: 'Date Time Picker',
  description:
    'Combined date and time entry for scheduled events, SLA timestamps, and audit fields requiring both calendar day and clock time.',
  componentSlug: 'date-time-picker',
})
