import { createExpertComponentPage } from '../shared/createExpertComponentPage'

export default createExpertComponentPage({
  slug: 'date-picker',
  title: 'Date Picker',
  description:
    'Single-date input with calendar popover — for deadlines, effective dates, and any field requiring one calendar day without time.',
  componentSlug: 'date-picker',
})
