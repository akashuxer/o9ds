/** o9con icon font glyph — bundled via site CSS (`main.jsx`). */
export default function O9conExternalLinkIcon({ className = 'arvo-icon-16' }) {
  return <span className={`o9con o9con-external-link shrink-0 ${className}`.trim()} aria-hidden />
}
