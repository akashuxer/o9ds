/**
 * Symbol (file type icons) — 24px (default) and 32px only; SCSS for Code / implementation snippets
 */
export const SYMBOL_SIZE_TOKENS_SCSS = `// Symbol — file type icons (24px default, 32px alternate)
$arvo-symbol-size-24: 1.5rem; // 24px — default
$arvo-symbol-size-32: 2rem; // 32px`
